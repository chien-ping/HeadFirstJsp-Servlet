除了pageContext，都是存放在作用域的屬性(Attribute)，而非物件的特性(property)，如果要取得物件的特性，需要透過pagContext
P388

對應各種作用域屬性的Map
pageScope
requestScope
sessionScope
applicationScope

動應各種請求參數的Map
param
paramValues

對應請求標頭的Map
header
headerValues

Cookie名值對所構成的Map
cookie

對應Context初始參數的Map
initParam

指向pageContext的實際參考
pageContext
